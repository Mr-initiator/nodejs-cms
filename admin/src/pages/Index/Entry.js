import router from './Router.js';

export default angular.module('index',[])
	.config(router)
	.name;































































